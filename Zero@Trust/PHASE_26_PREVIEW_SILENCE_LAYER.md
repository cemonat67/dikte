# Zero@Trust v2.6.1 — Preview Silence Layer
Status: Preview Stable
Core principle:
Local preview behaves like a complete atmosphere, not a broken deployment.
Implemented:
- Local preview detection for localhost, 127.0.0.1 and file protocol
- Zero network noise in preview mode
- No API fetch during static preview
- No favicon 404
- Silent trust state fallback
- Console calmness target: 0 red errors
Freeze rule:
No new UI widgets, charts, alerts, dashboards, or visible debugging layers.
